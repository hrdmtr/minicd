<div class="row mb-4">
  <div class="col">
    <h1>Edit Project</h1>
    <p class="lead">Update settings for <%= project.name %></p>
  </div>
</div>

<div class="row">
  <div class="col-md-8">
    <div class="card shadow-sm">
      <div class="card-body">
        <% if (error) { %>
          <div class="alert alert-danger" role="alert">
            <%= error %>
          </div>
        <% } %>
        
        <form action="/projects/<%= project._id %>/update" method="POST">
          <div class="mb-3">
            <label for="name" class="form-label">Project Name</label>
            <input type="text" class="form-control" id="name" name="name" required
                   value="<%= project.name %>" placeholder="e.g., my-web-app">
            <div class="form-text">A unique name for your project</div>
          </div>
          
          <div class="mb-3">
            <label for="repositoryUrl" class="form-label">GitHub Repository URL</label>
            <input type="url" class="form-control" id="repositoryUrl" name="repositoryUrl" required
                   value="<%= project.repositoryUrl %>" placeholder="e.g., https://github.com/username/repo.git">
            <div class="form-text">The URL to your Git repository</div>
          </div>
          
          <div class="mb-3">
            <label for="branch" class="form-label">Branch</label>
            <input type="text" class="form-control" id="branch" name="branch"
                   value="<%= project.branch %>" placeholder="e.g., main, master, production">
            <div class="form-text">The branch that will trigger deployments</div>
          </div>
          
          <div class="mb-3">
            <label for="port" class="form-label">Container Port</label>
            <input type="number" class="form-control" id="port" name="port" required
                   min="1" max="65535" value="<%= project.port %>">
            <div class="form-text">The port your application listens on inside the container</div>
          </div>
          
          <hr class="my-4">
          
          <h5 class="mb-3">Advanced Settings</h5>
          
          <div class="mb-3 form-check">
            <input type="checkbox" class="form-check-input" id="isActive" name="isActive" value="true"
                  <%= project.isActive ? 'checked' : '' %>>
            <label class="form-check-label" for="isActive">Active</label>
            <div class="form-text">Inactive projects won't be deployed automatically</div>
          </div>
          
          <div class="d-grid gap-2 d-md-flex justify-content-md-end">
            <a href="/projects/<%= project._id %>" class="btn btn-outline-secondary me-md-2">Cancel</a>
            <button type="submit" class="btn btn-primary">Update Project</button>
          </div>
        </form>
      </div>
    </div>
  </div>
  
  <div class="col-md-4">
    <div class="card shadow-sm bg-light">
      <div class="card-body">
        <h5 class="card-title">Notes</h5>
        <ul class="mb-0">
          <li class="mb-2">Changing the repository URL or branch will affect future deployments.</li>
          <li class="mb-2">If you change the port, make sure your application is configured to listen on this port inside the container.</li>
          <li class="mb-2">Setting a project to inactive will prevent automatic deployments but you can still deploy manually.</li>
        </ul>
      </div>
    </div>
    
    <div class="mt-4">
      <a href="/projects/<%= project._id %>" class="btn btn-outline-primary w-100">Back to Project</a>
    </div>
  </div>
</div>